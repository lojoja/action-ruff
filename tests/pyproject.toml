[project]
name = "action-ruff"
version = "0.0.0"
requires-python = ">=3.13,<4"
dependencies = [
  "ruff (==0.11.12)"
]

[build-system]
requires = ["uv_build>=0.7.1,<1.0.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 120

[tool.ruff.lint.mccabe]
max-complexity = 10
